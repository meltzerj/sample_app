<% if @user.errors.any? %>
  <div id="error_explanation">
    <h2>Woops! You made <%= pluralize(@user.errors.count, "error") %> 
        when signing up.</h2>
    <p>There were problems with the following fields:</p>
    <ul>
    <% @user.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>
